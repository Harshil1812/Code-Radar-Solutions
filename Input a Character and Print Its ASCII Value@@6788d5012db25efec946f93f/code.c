#include <stdio.h>

int main() {
    char character;
    scanf("%c", &character);
    printf("ASCII Value: %d\n", character); // %d is used to print the ASCII value of the character
    return 0;
}
