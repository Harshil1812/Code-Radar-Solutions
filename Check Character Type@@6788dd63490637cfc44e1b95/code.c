#include <stdio.h>
#include <ctype.h> /*for isdigit function*/
int main() {
    char x;
    scanf("%c",&c);
    if(x = a ,e , i , o , u){
        printf("Vowel");

    }
    else if(isalpha(x)){
        printf("Consatnt");
    }
    else if(isdigit(x)){
        printf("Digit");
    }
    else{
        printf("Special Character");
    }
    return 0;
}